# --- STREAMLIT SECRETS ---
# For local use: Rename this file to 'secrets.toml' 
# For Cloud use: Paste these values into the 'Secrets' box in settings.

OPENAI_API_KEY = "sk-your-openai-key-here"

[DB_CREDENTIALS]
# You can also group secrets into categories
user = "admin"
password = "my-secure-password"

# --- SECURITY WARNING ---
# 1. NEVER commit 'secrets.toml' to GitHub.
# 2. Add '.streamlit/secrets.toml' to your '.gitignore'.
